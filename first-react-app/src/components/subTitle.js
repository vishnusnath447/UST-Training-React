import React from "react";

const SubTitle = ()=>{
    const subTitleStyle = {
        color:"#766FFF"
    }
    return(
        <p style={subTitleStyle}>Displaying the list</p>
    );
};

export default SubTitle;