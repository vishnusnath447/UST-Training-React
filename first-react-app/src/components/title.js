import React from "react";

const Title = ()=>{
    const titleStyle = {
        color: "blue",
        fontSize: "2rem",
        fontWeight: "bold"
    };
    return(
        <h1 style={titleStyle}>React</h1>
    );
};

export default Title;